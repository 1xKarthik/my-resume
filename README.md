My resume build using AngularJs, take a look at here http://www.karthikraj.co.nf

clone the repo -> navigate to the project folder -> type `gulp serve` for live-reload or `gulp build` for production code.
